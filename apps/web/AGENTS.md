# Cinematic landing

Public internet. Product film, not a brochure. The only cloud-hosted surface.

- Stack: Vite + TypeScript + Three.js + GSAP. Scene graph is Three.js, not R3F.
- Never import Core crates, never call `:8443`, never ship home TLS material.
- Tour vignettes are authored. The film must play if any demo LLM is down.
- Phones: reduced glTF or video fallback. No 200MB first load.
- Milestone 4 may add add-on *purchase* on this origin. Activation is still a signed
  offline file the phone delivers (UC-401).
- Do not implement the villa walkthrough until this surface is explicitly in scope.
