[project]
name = "homeai-tts"
version = "0.0.0"
description = "TTS façade on :8300 — Chatterbox Multilingual primary, Piper fallback."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
